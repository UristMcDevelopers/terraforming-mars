go.property("size", vmath.vector3())

function init(self) 
	self.size = go.get("#sprite", "size")
end

function on_message(self, message_id, message, sender)
	if message_id == hash("set") then
		label.set_text("#label", message.action_name)
	end
end
